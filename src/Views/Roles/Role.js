import React, { Fragment } from "react";
import { Button, TextInput, View, Text, TouchableOpacity, FlatList} from "react-native";
import estilo from "./estilo.js"

const Roles = () => {
    const onPress = () =>{
        console.log("Clicou")
    }
    return(
        <Fragment>
            <FlatList>

            </FlatList>
            
        </Fragment>
    );

}

export default Roles;